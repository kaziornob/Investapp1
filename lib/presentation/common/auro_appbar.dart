import 'package:flutter/material.dart';

class AuroAppbar extends StatelessWidget {
  const AuroAppbar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
